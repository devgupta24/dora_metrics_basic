name: ETL Deployment Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # - name: Run Deployment
        # run: echo "Deployment Successful"
      # - name: Simulate Deployment Failure
      #   run: exit 1

      - name: Fixed Deployment
        run: echo "Deployment Fixed Successfully"